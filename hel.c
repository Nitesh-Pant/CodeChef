#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<maths.h>
int main()
{
	int t,n,i;
	scanf("%d",&t);
	while(t--)
	{
		scanf("%d",&n);
	 	char str[n];
	 	scanf("%s",&str);
	}
	for(i=0;i<n;i++)
	{
			int a;
			char str[n];
			printf("%s",str(n-1));
	}
getch();
return 0;
}
